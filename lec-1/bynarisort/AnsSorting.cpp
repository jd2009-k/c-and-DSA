#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, h, max = 0, sum = 0;
    cin >> n >> h;
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
        sum += v[i];
        if (max < v[i])
            max = v[i];
    }
    long long int lo = 1, hi = max;
    for (; lo <= hi;)
    {
        mid = (hi + lo) / 2;
        int sum1 = 0;
        for (; sum != sum1;)
    }
}